============
Installation
============

At the command line::

    $ easy_install django-publica-pages

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publica-pages
    $ pip install django-publica-pages
